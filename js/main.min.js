$(document).ready(function () {

    /* carousel for "most read block" */
    $(".events-slider").slick({
    arrows: false,
    dots: true,
    autoplay: true,
    slidesToShow: 1,
    autoplaySpeed: 3000
    });

});